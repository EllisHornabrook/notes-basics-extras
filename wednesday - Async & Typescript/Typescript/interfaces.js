var showList = function (todo) {
    console.log(todo.title + ": " + todo.text);
};
var myTodo = { title: 'Clean', text: 'Kitchen' };
showList(myTodo);
;
var showList = function (todo) {
    console.log(todo.title + ": " + todo.text);
};
var myTodo = { title: 'Clean', text: 'Kitchen' };
showList(myTodo);

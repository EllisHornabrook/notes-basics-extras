var funcOne = function (num1, num2) {
    return num1 + num2;
};
console.log(funcOne(2, 22));
var makeName = function (firstName, lastName) {
    return firstName + ' ' + lastName;
};
console.log(makeName('Ellis', 'Hornabrook'));
